[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
md_core = { path = "../core" }
thiserror = "2.0.18"
tracing = "0.1.44"
tokio = { version = "1", features = ["sync"] }
rust_decimal = "1.40"

[dev-dependencies]
rand = "0.8"